name: turbodex_app
description: TurboDex mobile app
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  share_plus: ^10.0.2
  url_launcher: ^6.3.0
  timeago: ^3.6.1
  collection: ^1.18.0
  image_picker: ^1.1.2
  # (ajoute d’autres libs ici si ton code les utilise, ex: camera, image_picker, etc.)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/

